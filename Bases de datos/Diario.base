filters:
  and:
    - creado.toString().contains(this.file.name)
views:
  - type: table
    name: Diario
    filters:
      and:
        - file.name != this.file.name
    order:
      - file.name
      - creado
      - categorias
      - tags
    sort: []
